# idle-cosmic

COSMIC product package (built from [idle-cosmic](https://github.com/idlescreen/idle-cosmic), not from this sketch).

```bash
sudo dnf install idle-cosmic
sudo apt install idle-cosmic
```

Ships the panel applet (`idlescreen-applet`). Requires `idle-daemon` / `idle-tui` per the published package metadata.
